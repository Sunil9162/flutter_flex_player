enum InitializationEvent {
  initializing,
  initialized,
  uninitialized,
}

enum PlayerState {
  playing,
  paused,
  stopped,
  buffering,
  ended,
}

enum ControlsStyle {
  defaultStyle,
  style1,
  style2,
  customStyle,
}
